{{- if .Values.ingress.sqs-producer.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.ingress.sqs-producer.name | quote }}
  labels:
  {{- include "sqs-app.labels" . | nindent 4 }}
  {{- with .Values.ingress.sqs-producer.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: alb
  rules:
  - host: {{ .Values.ingress.sqs-producer.dnsHostname | quote }}
    http:
      paths:
      - backend:
          service:
            name: {{ .Values.ingress.sqs-producer.name | quote }}
            port:
              number: {{ .Values.ingress.sqs-producer.port }}
        path: /
        pathType: Prefix
{{- end }}
