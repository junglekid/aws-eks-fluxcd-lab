{{- if .Values.ingress.sqsProducer.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.ingress.sqsProducer.name | quote }}
  labels:
  {{- include "sqs-app.labels" . | nindent 4 }}
  {{- with .Values.ingress.sqsProducer.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: alb
  rules:
  - host: {{ .Values.ingress.sqsProducer.dnsHostname | quote }}
    http:
      paths:
      - backend:
          service:
            name: {{ .Values.ingress.sqsProducer.name | quote }}
            port:
              number: {{ .Values.ingress.sqsProducer.port }}
        path: /
        pathType: Prefix
{{- end }}
